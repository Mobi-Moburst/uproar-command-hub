

## Expand search to cover all useful fields

**`src/pages/PlacementsPage.tsx`** — Update the search filter logic in the `filtered` useMemo to also match against `reporter_name`, `secured_by`, `vertical`, `type`, and `topic_product`.

Update the placeholder text to reflect the broader search capability.

